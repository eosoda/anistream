import Link from 'next/link';
import { ArrowRight, CircleAlert, CircleCheck, type LucideIcon } from 'lucide-react';
export interface OperationalItem { title: string; description: string; href?: string; status?: 'ok' | 'attention' }
export function OperationalPage({ title, description, icon: Icon, items }: { title: string; description: string; icon: LucideIcon; items: OperationalItem[] }) {
  return <div className="mx-auto max-w-6xl space-y-6"><header className="border-b border-[var(--border-subtle)] pb-5"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-[var(--radius-control)] bg-[#FF6B00]/12 text-[var(--accent)]"><Icon size={22} /></span><div><h1 className="text-2xl font-bold">{title}</h1><p className="mt-1 max-w-2xl text-sm text-[var(--text-secondary)]">{description}</p></div></div></header><div className="divide-y divide-[var(--border-subtle)] overflow-hidden rounded-[var(--radius-media)] border border-[var(--border-subtle)] bg-[var(--surface-1)]">{items.map((item) => { const content = <><span className={`grid size-9 shrink-0 place-items-center rounded-[var(--radius-field)] ${item.status === 'attention' ? 'bg-amber-500/10 text-amber-400' : 'bg-emerald-500/10 text-emerald-400'}`}>{item.status === 'attention' ? <CircleAlert size={18} /> : <CircleCheck size={18} />}</span><span className="min-w-0 flex-1"><strong className="block text-sm">{item.title}</strong><span className="mt-1 block text-sm text-[var(--text-secondary)]">{item.description}</span></span>{item.href && <ArrowRight size={18} className="shrink-0 text-[var(--text-muted)]" />}</>; return item.href ? <Link key={item.title} href={item.href} className="flex min-h-16 items-center gap-3 p-4 hover:bg-white/4">{content}</Link> : <div key={item.title} className="flex min-h-16 items-center gap-3 p-4">{content}</div>; })}</div></div>;
}
